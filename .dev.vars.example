# Copy this file to .dev.vars for local `wrangler dev`.
# NEVER commit real keys. In production set these with `wrangler secret put`.

# Anthropic API key — powers the Living Hope Map matcher and the article draft helper.
ANTHROPIC_API_KEY=""

# News API key (e.g. newsapi.org / GNews) — themed local-news lookup for the Hope Map.
NEWS_API_KEY=""
